# 📁 依存関係（ローカルのみで保持、Git管理外）
node_modules/

# 🔒 環境変数・秘密鍵（絶対にGitHubに上げてはいけない最重要防衛ライン）
.env
.env.production
.env.local
.env.*.local

# 🚀 ビルド出力（Cloudflare Pages側が自動生成するため不要）
dist/
.astro/

# 🛠️ 各種ログやキャッシュ
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
.DS_Store